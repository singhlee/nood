spring:
  application:
    name: nood-gateway
  http:
    encoding: { charset: UTF-8, enable: true, force: true }
  cloud:
    config:
        uri: http://localhost:8762
  security:
    user:
      name: admin
      password: 1qaz2WSX