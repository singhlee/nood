spring:
  application:
    name: nood-auth
  http:
    encoding: { charset: UTF-8, enable: true, force: true }
  cloud:
    config:
      uri: http://localhost:8762
      profile: dev
  security:
    user:
      name: admin
      password: 1qaz2WSX


