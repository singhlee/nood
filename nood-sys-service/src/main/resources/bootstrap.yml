spring:
  application:
    name: nood-sys-service
  http:
    encoding: { charset: UTF-8, enable: true, force: true }
  cloud:
    config:
      uri: http://localhost:8763/
      name: sysservice
      profile: ${config.profile:dev}

info:
  name: nood-sys-service
  author: singh