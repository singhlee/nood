spring:
  application:
    name: nood-config
  cloud:
    config:
      label: master
      server:
        git:
          timeout: 60000
          clone-on-start: true
          search-paths: /*
          uri: https://github.com/jackelee1990/nood-config-repo.git
          username: jackelee1990
          password: 3edc#EDC
  http:
    encoding: { charset: UTF-8, enable: true, force: true }
info:
  name: nood-config
  author: singh